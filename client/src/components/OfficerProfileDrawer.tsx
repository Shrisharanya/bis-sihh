import { BadgeCheck, BriefcaseBusiness, CheckCircle2, LockKeyhole, X } from "lucide-react";
import type { Language } from "../types";
import { tx } from "../utils/translations";
interface OfficerProfileDrawerProps { open: boolean; onClose: () => void; language: Language; onLoadDraft: (standard: string) => void; }
export default function OfficerProfileDrawer({ open, onClose, language }: OfficerProfileDrawerProps) {
  if (!open) return null;
  return <aside className="workspace-profile-popover workspace-profile-details" role="dialog" aria-label={tx(language, "openProfile")}><div className="popover-header"><div><span className="drawer-kicker"><BadgeCheck size={12} /> NIC CREDENTIALLED SESSION</span><h2>{tx(language, "officer")}</h2><p>{tx(language, "officerRole")} · {tx(language, "department")}</p></div><button className="icon-button" onClick={onClose} aria-label={tx(language, "close")}><X size={15} /></button></div><div className="credential-card"><div className="credential-icon"><LockKeyhole size={16} /></div><div><strong>{tx(language, "activeCredential")}</strong><span>Issued by National Informatics Centre · Last verified 22 Sep 2026</span></div><CheckCircle2 size={14} /></div><div className="profile-detail-grid"><div><span>OFFICER ID</span><strong className="mono">#GOV-8941</strong></div><div><span>DESK</span><strong>PWD / GeM</strong></div><div><span>SESSION</span><strong>Private</strong></div><div><span>ACCESS</span><strong>Source-locked</strong></div></div><div className="popover-footer"><BriefcaseBusiness size={13} /><span>{language === "hi" ? "यह अधिकारी सत्र निजी है।" : "This officer session is private."}</span></div></aside>;
}
